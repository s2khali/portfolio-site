import Link from 'next/link';

const NavBar = (props) => {
    return (
        <StyledNavBar>
            <Link href={}>About</Link>
            <Link href={}>Portfolio</Link>
            <Link href={}>Contact</Link>
        </StyledNavBar>
    )
}
